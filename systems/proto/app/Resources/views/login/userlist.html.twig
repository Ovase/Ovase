{# app/Resources/views/login/userlist.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <section class="medium-wide centered">
	{% if babes is defined %}
		{% for element in babes %}
			Profil: <a href="{{ path('publicprofile', {'id': element.id}) }}">{{element}}</a>
			{% if babes is defined %}
				- <a href="{{ path('editotherprofile', {'id': element.id}) }}">Rediger</a> <br>
			{% endif %}
		{% endfor %}
    {% endif %}
    </section>
{% endblock %}